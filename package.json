{
  "name": "sccl-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "./bin/zine",
    "build": "./bin/zine release",
    "preview": "./bin/zine release && npx serve public"
  }
}

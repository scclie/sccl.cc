{
  "name": "sccl-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "/tmp/zine",
    "build": "zine release",
    "preview": "zine release && npx serve public"
  }
}

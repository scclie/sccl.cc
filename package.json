{
  "name": "sccl-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "/tmp/zine",
    "build": "/tmp/zine release",
    "preview": "/tmp/zine release && npx serve public"
  }
}
